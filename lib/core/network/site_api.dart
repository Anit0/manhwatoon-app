/// Compatibility shim. The generic source implementation now lives in
/// `core/sources/madara_source.dart`; keep this file so stale imports (and
/// `SiteApiException`) keep resolving.
library;

export '../sources/madara_source.dart';
